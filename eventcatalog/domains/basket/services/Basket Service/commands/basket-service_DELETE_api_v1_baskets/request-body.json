{
  "required": ["items"],
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "required": ["id", "quantity"],
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "The ID of the product to remove from the basket"
          },
          "quantity": {
            "type": "integer",
            "format": "int32",
            "description": "The quantity of the product to remove from the basket"
          }
        }
      }
    }
  }
}
